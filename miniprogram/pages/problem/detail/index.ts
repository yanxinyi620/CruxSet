// @ts-nocheck
import { demoWall } from '../../../data/demo.js';import { demoProblems } from '../../../data/demo-problems.js';import { getProblem,listProblems } from '../../../services/problems.js';import { getWall } from '../../../services/walls.js'
const rules={feet_follow:['手脚同点','手类点可踩，黄色 Foot 只能脚踩'],specified:['指定脚点','脚只能踩线路中的黄色 Foot'],all:['全墙脚点','当前墙面所有允许踩的岩点均可作为脚点']};const apply=(page,p,previous,next)=>{const active={};Object.entries(p.holds).forEach(([role,ids])=>ids.forEach(id=>active[id]=role));page.setData({problem:p,activeHolds:active,footRuleLabel:rules[p.footRule][0],footRuleHint:rules[p.footRule][1],previous,next})}
Page({data:{problem:null,wall:demoWall,activeHolds:{},footRuleLabel:'手脚同点',footRuleHint:rules.feet_follow[1],previous:null,next:null},onLoad(options){getProblem(options.id).then(async problem=>{const [wall,problems]=await Promise.all([getWall(problem.wallId),listProblems({wallId:problem.wallId,angle:problem.angle,grade:problem.grade})]);const i=problems.findIndex(p=>p.id===problem.id);this.setData({wall});apply(this,problem,problems[i-1],problems[i+1])}).catch(()=>apply(this,demoProblems[0],null,demoProblems[1]))},open(e){wx.redirectTo({url:`/pages/problem/detail/index?id=${e.currentTarget.dataset.id}`})},onShareAppMessage(){return{title:`${this.data.problem.number} · ${this.data.problem.name}`,path:`/pages/problem/detail/index?id=${this.data.problem.id}`}}})
