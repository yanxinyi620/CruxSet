// @ts-nocheck
import { getProblem, listProblems } from '../../../services/problems.js'
import { getWall } from '../../../services/walls.js'
import { cloudErrorMessage } from '../../../services/errors.js'
const rules={feet_follow:['跟随手点','手类点可踩，黄色 Foot 只能脚踩'],specified:['指定脚点','脚只能踩线路中的黄色 Foot'],all:['全墙脚点','当前墙面所有允许踩的岩点均可作为脚点']}
Page({data:{problem:null,wall:null,activeHolds:{},footRuleLabel:'跟随手点',footRuleHint:rules.feet_follow[1],previous:null,next:null,error:''},onLoad(options){getProblem(options.id).then(async p=>{const [wall,problems]=await Promise.all([getWall(p.wallId),listProblems({wallId:p.wallId,angle:p.angle,grade:p.grade})]);const active={};Object.entries(p.holds).forEach(([role,ids])=>ids.forEach(id=>active[id]=role));const i=problems.findIndex(x=>x.id===p.id);this.setData({problem:p,wall,activeHolds:active,footRuleLabel:rules[p.footRule][0],footRuleHint:rules[p.footRule][1],previous:problems[i-1],next:problems[i+1]})}).catch(error=>this.setData({error:cloudErrorMessage(error)}))},open(e){wx.redirectTo({url:`/pages/problem/detail/index?id=${e.currentTarget.dataset.id}`})},onShareAppMessage(){return{title:`${this.data.problem.number} · ${this.data.problem.name}`,path:`/pages/problem/detail/index?id=${this.data.problem.id}`}}})
