export type PreviewRoute = { name: 'browse' } | { name: 'create' } | { name: 'me' } | { name: 'wall'; wallId: string } | { name: 'draft-editor'; wallId: string; layoutId: string } | { name: 'problem-editor'; wallId: string; layoutId: string } | { name: 'problem-detail'; problemId: string }

export const toPreviewUrl = (route: PreviewRoute) => route.name === 'browse' ? '/' : route.name === 'create' || route.name === 'me' ? `/${route.name}` : route.name === 'wall' ? `/wall/${encodeURIComponent(route.wallId)}` : route.name === 'problem-detail' ? `/problem/${encodeURIComponent(route.problemId)}` : `/${route.name}/${encodeURIComponent(route.wallId)}/${encodeURIComponent(route.layoutId)}`
