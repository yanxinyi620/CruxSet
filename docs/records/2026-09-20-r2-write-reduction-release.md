# 2026-09-20 R2 写入去重上线

- 功能提交：`8d6b0f3`，已推送至 `origin/cloudflare`。
- 生产地址：https://cruxset.xinyilab.top。
- Worker 版本：`dd09909e-d19a-4e1d-b7ff-3c183fddf545`。
- 校准保存按内容去重；已满配额的图片上传和发布提前拒绝；发布申请先取得锁再写快照；运行和重试按钮增加提交保护，服务端按提交标识去重。

## 发布与验证

- 生产数据库依次应用 `0018_lab_calibration_save_dedup.sql`、`0019_lab_submission_dedup.sql`，均成功；复查无待应用迁移。
- 新增字段兼容旧版本显式列名插入，先迁移再部署。
- 迁移前 D1 Time Travel bookmark：`000009d2-00000000-000050ec-086c2fdc50ab9b6aee57f96280bdf422`。
- 576 项自动化测试通过，仓库和 Edge 类型检查、Web 构建、Worker dry-run 通过。
- Python 33 项测试通过；排除此前在旧版本复现的默认模型阈值断言失败 1 项。
- 线上首页、实验台、校准页面均返回 200，内容与构建产物逐字节一致。
- healthz 返回 `ok`，bootstrap 返回 200，未登录实验列表返回预期 401。
- 并发写入、提交重放和失败恢复由自动化测试验证，未在线上创建真实模型任务。

## 回退

可保留新增数据库结构，将 Worker 回退至前一生产版本 `090cbc48-a033-4ba5-9097-3c0f476bbede`。回退后本次写入去重优化失效，之前的清理调度优化仍保留。
