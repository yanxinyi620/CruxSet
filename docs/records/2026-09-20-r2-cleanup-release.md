# 2026-09-20 R2 清理调度上线

- 功能提交：`bcd0bdd`，已推送至 `origin/cloudflare`（包含此前未推送的快照清理提交 `16f2e93`）。
- 生产地址：https://cruxset.xinyilab.top。
- 正常目录按首次、2 小时、24 小时执行清理；定时器仍为每 5 分钟，仅处理到期记录。
- 已知岩壁图片、发布快照直接删除；目录每批最多 500 个对象，分批继续。失败按 5、15、30、60 分钟退避，最长每小时重试。
- 删除接口只清理当前目标；执行锁与版本检查保护并发，迟到上传重新唤醒清理并延长保护窗口。

## 发布顺序

1. 原生产版本：`c7271243-4299-488a-bd10-a95d6cde2e2f`。
2. 从 `16f2e93` 构建兼容版本，仅将三处两值 `lab_gc` 插入改为显式列名。兼容版类型检查、68 项 Edge 测试通过；部署版本 `3f0b95cb-a84a-4015-8e39-cc3233b1ff9c`，健康检查正常。
3. 迁移前远端查询显示活跃分割任务为 0，清理记录为 0；兼容部署后等待旧请求退出并记录 D1 Time Travel 恢复点。
4. 2026-09-20 09:28:42 UTC 应用 `0017_lab_gc_schedule.sql`，11 条语句全部成功。查询 `d1_migrations` 和表结构确认完成。
5. 部署最终 Worker：`090cbc48-a033-4ba5-9097-3c0f476bbede`，主域、API 子域、workers.dev 和五分钟定时器均已配置。

## 验证

- 完整测试：93 个文件，548 项全部通过。
- 仓库、Edge 类型检查与生产 Web 构建通过。
- 首页、我的墙面入口、实验台、healthz、bootstrap 返回 200，healthz 状态为 `ok`。
- 未登录访问发布申请返回预期的 401。
- 主站 index.html、实验台 index.html 和 runtime.js 与本次构建逐字节一致（跟随规范路径重定向）。
- 迁移后清理队列为空；未创建或删除真实业务数据来验证清理行为。三次复查、失败退避、并发与迟到上传行为由自动化测试验证。

- 线上版本的定时任务在 `2026-09-20T09:30:37+00:00` 执行结果为 `ok`，异常数和错误日志数均为 0。

## 回退

保留新增数据库字段。若需要回退 Worker，使用兼容版本 `3f0b95cb-a84a-4015-8e39-cc3233b1ff9c`；不要回退到仍使用两值位置插入的原生产版本。兼容版恢复旧的高频清理行为，仅用于应急回退。
