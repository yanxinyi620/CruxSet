# Web 线路详情统一

用户已认可：本地 Web 与云端 Web 共用完整详情；我的线路可以点击查看，返回恢复我的线路。

两种构建共享 web/src/main.ts。移除独立简版渲染，保留 /problem/:id 兼容地址，加载后替换为该墙面浏览器的选中线路地址。我的线路入口携带 from=my-problems 与返回墙面标识，刷新及上下条切换保持来源。顶部返回和返回列表均恢复我的线路并展开原墙面。公共浏览维持筛选、上下条、全屏及返回列表。我的线路入口清除公共浏览残留筛选，上下条沿用当前墙面全部线路。失效独立地址显示错误及返回入口。
