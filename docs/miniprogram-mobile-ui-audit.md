# 小程序手机 UI 对齐记录（第一轮）

基准：本机 Web 390×844 视口；不含实验台。不改动 Web。

## 已调整

- 墙面首页、概览、线路列表和详情：统一手机端字号、间距、卡片、图例和导航按钮；墙面概览按原图比例显示画布。
- 创建、选墙、线路编辑、上传及标注：压缩无效留白，统一入口、工具及保存面板布局。
- 我的、资料、墙面、线路、管理和草稿：统一列表及操作层级，区分编辑和删除，修复部分加载期间空态闪现。
- 保留缓存及 0–70° 筛选改造。

## 验证

334 项测试（72 个文件）通过；主工程和小程序 TypeScript 检查通过；15 个页面入口编译生成成功；微信预览编译通过，包大小 129.3 KB。最新代码已同步至 C:/Users/yanxi/CruxSet-debug-20260911。

## 2026-09-12 补充验收

- 已恢复自动连接，重新截图全部 15 页；每页业务错误字段为空。
- 创建页管理员入口完整；详情页返回按钮占满内容宽度；编辑器角色标签已中文化。
- 补查全屏预览及保存面板，按钮和输入区完整可见；未执行确认保存或发布。
- 原生画布像素读取证明已绘制；导出 PNG 后确认墙图及绿色起点、紫色终点可见。页面截图通道遗漏画布，不能用灰色截图判断真实绘制。画廊分别展示原始页面截图及画布导出图。
- 临时诊断只注入 Windows 生成文件，现已重新构建移除；不进入源码。

## 管理员登录后补充对照

已按 390×844 补齐 Web 的创建、选墙、编辑、我的、资料、我的墙面、我的线路、管理、上传、草稿和墙面编辑截图，15 个页面均有 Web 参考。

最后两页已补拍，业务错误字段为空。

新增调整：我的墙面采用紧凑横向卡片，删除按钮移到右侧；草稿入口使用与 Web 一致的浅绿色背景。事件绑定与权限逻辑未改变。

保留的平台差异：Web 邮箱账号与小程序微信身份不同；小程序上传要求墙面名称；原生返回导航与 Web 底部导航不同；实验台及其授权不在范围内。

数据覆盖限制：小程序没有私有草稿，墙面编辑截图为公开墙面锁定状态，Web 为私有草稿，未为截图创建业务数据。截图工具遗漏原生画布，已用独立导出图核对墙图和岩点。

验证：334 项测试、TypeScript 检查与微信预览编译通过，包大小 130.2 KB。未发布正式版。

截图画廊：../output/playwright/ui-comparison.html。

## 统一源码管理

源码已合入原项目 `/home/yanxi/code/project/CruxSet`。后续在原项目修改、测试并提交；Windows 目录仅存放生成后的调试代码。

仅在需要旧的备用编译目录时，从原项目根目录手动生成（直接源码调试不需要此步骤）：

```sh
node scripts/build-wechat-preview.mjs /mnt/c/Users/yanxi/CruxSet-debug-20260911
```

`output/playwright/` 是本地截图对照资料，不纳入 Git；`.runtime/` 是临时诊断与运行文件。

## 直接从原项目调试（推荐）

在 Windows 微信开发者工具中手动导入：

```text
\\wsl$\Ubuntu-24.04\home\yanxi\code\project\CruxSet\wechat
```

用户已确认该路径可手动导入。选择 `wechat` 目录，不是其下的 `miniprogram`。项目已启用 TypeScript 编译，AppID 为 `wx123fe6920af8c5a9`。

这就是原项目源码，无需维护副本，也无需同步到旧 Windows 调试目录。源码修改后点击开发者工具“编译”，再启动真机调试；如果文件监听未自动触发，手动编译即可。

验证范围：相同源码通过盘符别名的命令行预览编译；GUI 的 WSL 路径导入由用户确认。当前版本的命令行接口拒绝 UNC 路径，不代表手动导入界面也不支持。无需额外映射盘符或启动脚本。

旧 `C:\Users\yanxi\CruxSet-debug-20260911` 目录仅作为备用，后续以原项目 `wechat` 为准。
