# 云端实验台链路验证

## 本地 Worker 运行时与真实模型

2026-09-11 使用 Wrangler 的真实 workerd/D1/R2 本地模拟环境、浏览器和新增 CloudRunner 验证链路。采用独立本地数据库和测试管理员，不读取或修改生产墙面。GitHub dispatch 使用无效测试凭据验证失败反馈，随后只在本地测试数据库重新排队，由本机执行器领取；此过程不代表 GitHub Actions 远端验证成功。

- 浏览器从仓库墙图手动选择四角并上传，裁剪后尺寸 1533×1277。
- 使用当前采样修正版 SAM2.1 large，固定 revision `665f8e2ad61cf5f53d65644ff27c8ee525124610`，缓存权重、CPU 4 线程。
- 参数：点密度 8、批量 8、IoU .85、稳定度 .90、内部裁剪层 0。
- 执行器完成领取、输入校验、真实推理、三个输出文件上传和成功回调，耗时 22.74 秒，峰值 RSS 1756.67 MiB，最终 40 个候选。
- 浏览器加载真实候选，人工新增三角轮廓，保存为 41 个候选的独立校准。
- SVG 导出通过 XML 解析验证，包含 41 个 polygon。
- 浏览器发布校准，D1 创建公开墙面和 41 个岩点，R2 保存独立公开展示图。

这是一轮低点密度连通性验证，不是默认点密度 48 的性能基准，也不能用于估计标准 GitHub runner 的耗时或 4K 分块模式峰值。

## 4K 分块推理补测

同日本机使用仓库原图 `tests/fixtures/ritan-spraywall-0822.jpg`（4096×3072），对当前采样修正版 `sam2_tiled` 运行真实 CPU 推理。4 线程、点密度 8、批量 8、IoU .85、稳定度 .90、内部裁剪层 0，缓存模型权重。

- 耗时 91.40 秒，峰值 RSS 2303.40 MiB（约 2.25 GiB）。
- 输出 115 个候选多边形，保留掩码数据 1,810,373 字节。

该结果验证了此参数组合下的本机分块推理内存，不包含 GitHub 排队、环境安装、权重下载或云端传输，也不代表默认点密度 48 的资源需求。

## 验证中修复的问题

- Mask ZIP 经普通 TransformStream 处理后会失去 R2 要求的流长度元数据；改为检查 Content-Length 上限并直接传输原请求流，保持低内存且通过真实 R2 模拟运行时。
- 校准页面拦截全局 fetch 会递归进入共享请求函数；所有请求改为显式 Lab.request，并增加实际执行保存处理器的回归测试。
- 修正云端发布目标默认值、已删除来源任务的校准列表、发布墙面的 pathname 链接和动态页面的静态资源映射。

## 自动测试

全量 TypeScript 测试、Python 实验台测试、Web/小程序类型检查、Worker 类型检查、Web 构建与 Worker dry-run 均单独执行。具体最终数量和远端验证结果以本任务交付记录为准；不要把本地模拟环境的通过当作远端上线证据。
